# -*- mode: snippet -*-
# name : printf 
# contributor : joaotavora
# --
printk("@@link@@:%s:%d:${1:%s}\\n", __FILE__, __LINE__${1:$(if (string-match "%" text) "," "\);")
}$2${1:$(if (string-match "%" text) "\);" "")}
